# INTT Case

本样例用于交付一份完整的 `INTT` 指令文件和对象级输入/输出约束。

目录内容：

- `intt.asm`：N=64 的 stage 级 INTT 指令
- `input.txt`：对象槽位分配和输入多项式
- `expected.txt`：期望输出槽位和校验规则

约束说明：

- `p0` 为 NTT 域输入对象
- `p1` 为 ping-pong 临时对象
- `p2` 为模上下文对象
- `p3` 为 shuffle 配置对象

与 `ntt/` 相同，当前公开文档不足以推出可靠的数值黄金值，因此本样例保持接口完整、校验规则完整。
