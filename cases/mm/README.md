# MM Case

本样例给出一组可以直接人工验算的 `pmul` 用例。

目录内容：

- `mm.asm`：模上下文装载 + `pmul` + `psync`
- `input.txt`：`p0`、`p1` 输入和 `p3` 模上下文
- `expected.txt`：`p2` 的黄金输出

校验逻辑：

- 指令应在 `p3` 模上下文下执行
- `pmul p2, p0, p1` 的结果应写入 `p2`
- 本例使用 `q=257`，输入较小，不会触发模回绕
